# tombstone (가칭) — 기각된 시도를 기억하는 에이전트 부(負)메모리

- **작성일**: 2026-09-05
- **목적**: 에이전트 세션·리뷰·git 이력에 흩어진 "실패·기각" 지식을 레포 귀속 구조 기록으로 영속화하고, 다음 세션의 에이전트가 재시도 **전에** 조회하게 하는 오픈소스 기획
- **선점 여부 검증**: 2026-09-05 웹 검색. 1차 검증에서 "rejected patch triage" 담론 글만 존재하고 도구 0건으로 확인됐으나, 2차 정밀 검증에서 **전역 에러 시그니처 계층은 [deadends.dev](https://github.com/dbwls99706/deadends.dev)가 선점**(2,089 에러 · 51 도메인 · MCP 9툴 · PyPI 배포)으로 확인. 그러나 그쪽은 "에러 메시지 → 전 세계 공통 교훈"이고, **"이 레포·이 모듈에서 시도된 접근의 기각 원장" 계층은 미점유** → 빈 땅 등급 **⭐⭐☆** (1단 하향. 경위는 §차별화에 정직하게 기록)

---

## 한 줄 정의

> 모든 에이전트 메모리는 성공과 선호만 축적한다. tombstone은 기각된 패치·롤백된 접근·포기한 경로를 구조화된 묘비로 남기고, 다음 에이전트가 같은 길을 걷기 전에 조회하게 한다 — "이 접근, 8월 12일에 이 모듈에서 묻혔다."

---

## 문제 정의: 실패 지식의 3중 소실

1. **git 이력**: `Revert "Add Redis lock"` — 무엇을 되돌렸는지는 남지만, 왜 / 어떤 조건에서 / 무엇이 대안인지는 소멸
2. **리뷰**: PR close, 리뷰 스레드 속 반대 사유는 검색 불가능한 형태로 매장
3. **세션**: 에이전트가 "다른 방법으로 접근하겠다"고 포기를 선언한 전환점은 트랜스크립트에만 존재, 세션 종료와 함께 소멸

결과: yield-audit M9(세션 간 반복 지식 비용)의 최악 항목 — **같은 실패의 재발견**. 인간 팀은 이걸 두뇌와 구전으로 해결했다("그거 지난번에 해봤는데 안 돼"). 에이전트 시대에는 그 두뇌가 어디에도 저장되지 않는다.

---

## 데이터 모델: 묘비 1장 = YAML 1개

```yaml
id: ts-2026-0812-a3f
attempt: "Redis 기반 분산 락으로 세션 직렬화"
scope: [src/session/lock.py, src/session/manager.py]   # 심볼/파일 앵커
rejected_at: 2026-08-12
rejected_by: human-review        # human-review | ci | agent-gaveup
reason: "레이스 윈도우 미제거(재시도 폭주), 플레이크 테스트 3개"
evidence: [PR #412, revert 9f2e1a, session 2026-08-12T14:22]
retry_when: "fencing token 도입 전제로 재검토 가능"
status: active                   # active | stale(대상 코드 소멸) | overturned(재시도 성공)
confidence: approved             # approved | candidate(미승인 후보)
```

설계 결정:

- **파일 1개당 묘비 1개** — git 병합 충돌 최소화, 부분 채택 용이
- **레포 내 `.tombstones/` 기본 커밋** — 팀 공유가 핵심 가치. 개인용은 gitignore 선택
- **status 3상** — `overturned`는 반증 기록의 보존(실패 데이터셋의 정직성): "재시도했더니 됐다"도 중요한 지식

---

## 워크플로: 감지 → 초안 → 승인 → 조회

| 단계 | 담당 | 내용 |
|---|---|---|
| 감지 | 결정적 규칙 (LLM 불필요) | git revert 커밋 / never-merged PR close / 세션 포기 전환점 / CI 연쇄 실패 후 브랜치 폐기 |
| 초안 | LLM (옵션) | diff + 컨텍스트에서 attempt/reason draft. 없어도 성립 |
| 승인 | 사람 1줄 | `tombstone approve ts-...`. **에이전트가 스스로 판결하지 못한다**가 원칙 — 묘비는 증언이지 판결이 아니다 |
| 조회 | MCP 툴 2개 | `check_nogo(attempt\|files)`, `recent_tombstones(scope)` + AGENTS.md 유도 규칙 1줄 동봉 |

---

## 제품 원칙 (context-guard·yield-audit의 문법 계승)

- **완전 로컬 기본**: 묘비는 레포/기기에만 존재. 네트워크 호출 0
- **묘비는 단정하지 않는다**: "기각됐다"만 기록, "불가능하다"는 주장 안 함. `retry_when`(반증 조건)을 명시 의무화
- **승인 없는 후보 허용**: 마찰을 죽이지 않는다. 미승인 `candidate`는 조회 시 낮은 confidence로 노출
- **유통기한 의무화**: stale 감사로 대상 코드가 사라진 묘비는 자동 만료 (§v0.3)

---

## 아키텍처 (v0.1)

```
tombstone watch (git hook / CI)          tombstone query (MCP / CLI)
┌────────────────────────────┐           ┌─────────────────────────────┐
│ 감지기  revert·폐기·포기 전환점│           │ check_nogo(attempt|files)    │
│ drafter LLM 사유 초안(옵션)   │ ────────▶ │ recent_tombstones(scope)     │
│ approver 사람 1줄 승인       │  .tombstones/*.yaml       │
└────────────────────────────┘           │ stale 감사 (심볼 그래프, v0.3) │
                                         └─────────────────────────────┘
저장: 완전 로컬. 언어: Python 3.12+ (yield-audit과 어댑터·이벤트 모델 공유)
```

---

## 로드맵

```
v0.1  스키마 + CLI(add/approve/list/query/check) + git revert 감지기 + Claude Code MCP server
      — draft 없이도 성립하는 "결정적 최소 원장"
v0.2  세션 포기 전환점 감지 (yield-audit 트랜스크립트 어댑터 재사용)
      check_nogo 정교화 (정규화 문자열 매칭 → 임베딩 유사도 옵션), AGENTS.md 스니펫 생성
v0.3  stale 감사 — IndexStoreDB(iOS)/Kotlin 심볼 그래프로 묘비가 가리키는 심볼 생존 확인
      ← 제작자 정적분석 역량이 직접 먹히는 차별화 지점. overturn 흐름
v1.x  팀 모드 (묘비의 PR 리뷰 흐름), deadends.dev 상호운용(canon ID 링크), 기각 패턴 마이닝
```

---

## 차별화·경계 선언 (README에 명시할 것)

| 인접 | 그들과의 차이 |
|---|---|
| **deadends.dev** | 전역·**에러 시그니처** 중심("CUDA OOM이 뜨면 sudo pip 하지 마") — 레포 비귀속, 커뮤니티 큐레이션, 수동 신고. 우리: 레포 귀속·**시도(접근) 단위**·자동 감지·사람 승인·유통기한. **경쟁이 아니라 두 계층** — 에러 시그니처는 그쪽, 프로젝트 맥락의 기각은 우리. v1.x에서 canon ID 링크로 상호위임 |
| Mem0 / Letta / CLAUDE.md 메모리 | 성공·선호의 양의 메모리. 부의 메모리는 별도 자료구조여야 조회 패턴이 다르다 (작업 계획 단계의 사전 조회) |
| ADR (Architecture Decision Record) | 인간 설계 결정의 수동 문서. 우리는 시도의 기각 기록, 자동 감지 + 기계 조회가 전제 |
| .cursorrules 부정 규칙 | 맥락 없는 명령("X하지 마"). 묘비는 증거·조건·유통기한을 가진 기록 |
| yield-audit M9 | 측정(반복 세금의 크기) ↔ tombstone(그 세금의 최고액 항목 제거). 상호 판매 구조 |

---

## 리스크와 대응

- **콜드스타트**(승인을 안 하면 아무것도 안 쌓임) → revert 감지 100% 자동 + `candidate` 상태 저장 + 승인 마찰 1줄로 최소화
- **오운용**(묘비 남발 → 위경비 오염) → 묘비는 강제가 아닌 증언 / stale 자동 만료 / overturned 반증 보존 / 묘비 밀도 경고
- **채택 루프**(에이전트가 조회를 안 함) → MCP 툴 description 설계가 제품의 절반. AGENTS.md 규칙 동봉
- **프라이버시**(사유에 민감 정보) → 커밋 전 시크릿 패턴 스크럽 검사, 로컬 우선
- **선점 가속**(deadends.dev가 레포 로컬 계층으로 확장할 가능성) → 차별점을 심볼 그래프 연동(stale 감사)에 두면 정적분석 자산이 방어선

---

## 이름 후보

`tombstone`은 Android 네이티브 크래시 덤프·Cassandra tombstone과 충돌 — 제작자 도메인이 모바일이라 검색 오염이 특히 크다. 후보:

1. **`epitaph`** (묘비명 — "사유가 핵심"인 설계와 의미 정합. 2026-09-05 검색상 개발 도구 무충돌 확인)
2. `graveyard`
3. `deadend` 계열은 deadends.dev와 혼동되므로 배제

저장소 개설 시 GitHub/PyPI 가용성 확인 후 확정.

---

## 첫 공개 전략

자기 저장소의 **실제 묘비 10장**을 README에 싣는다. 후킹 한 줄("에이전트에게 전과를 보여줘라") + MCP 설정 3줄. 데모 GIF: 같은 작업을 tombstone 유무로 두 번 돌려 재시도 낭비를 대조. yield-audit M9 리포트("세션 간 반복 지불 토큰")를 전 증거로 함께 인용.

**포트폴리오 내 위치**: context-guard(입력 위생) → yield-audit(운명 측정) → tombstone(부메모리 처방의 첫 걸음). M9가 이 도구의 존재 증명이고, 이 도구가 M9를 낮추는 유일한 오픈소스 답이다.
